<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:required="false"
        android:version="0x00401000" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Downloading game patches from the shadps4 ps4_cheats repo. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- The native emulator core opens game files (eboot.bin, sce_sys/param.sfo, icon0.png) through
         raw filesystem paths (std::filesystem/fopen), which cannot read a content:// SAF URI. It
         therefore needs broad file access: MANAGE_EXTERNAL_STORAGE on Android 11+ (granted via a
         system settings screen), and the legacy READ/WRITE permissions on Android 10. Without it the
         games show no cover and fail to launch. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="SharpEmu"
        android:appCategory="game"
        android:isGame="true"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.SharpEmuAndroid">
        <meta-data
            android:name="android.game_mode_config"
            android:resource="@xml/game_config" />

        <!-- GameActivity is no longer declared here: the real emulation/rendering
             Activity is implemented in C# (SharpEmu.Android's GameActivity.cs,
             [Activity(Name = "org.sharpemu.android.GameActivity", Process = ":game", ...)]),
             since it hosts SDL3 + SharpEmu's managed core directly rather than
             going through this library's Kotlin/native code. MainActivity
             launches it via an explicit Intent naming that class, the same way
             it would launch any other Activity in this package regardless of
             implementation language. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|density|fontScale|locale|layoutDirection">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lets the in-game "export logs" button share shad_log.txt with other apps via a
             content:// URI without needing storage permissions. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove" />
    </application>
</manifest>
